/*
package chap1_3;

public class VehicleMain {
    public static void main(String[] args) {

        // 코드는 = 기준 오른쪽 영역이 먼저 실행 된다.
        // new xxx()가 실행 되면 메모리 힙 영역에 주소값을 가진 객체가 생성된다. (동적 메모리가 할당 된다.), 초기값은 기본값으로 세팅 된다.
        // 스텍에 변수 aaa가 생기고 xxx의 주소값을 포인터로 가르키고 있다.
        // new xxx()은 자동으로 기본 생성자를 만들어준다.
        Vehicle grandeur = new Vehicle("현대", "그랜져");
        Vehicle panamera = new Vehicle("포르쉐", "파나메라");

        grandeur.powerOn(); // grandeur만 시동이 걸린 거지 panamera은 시동이 걸린 게 아니다.

        System.out.println("grandeur = " + grandeur);
        System.out.println("panamera = " + panamera);
    }
}*/
